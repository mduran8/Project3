 Program 2 CS 478
 Authors: Melissa Duran
 Date: April 10, 2018


How to use:
run make with following command
make


Then:
code for encryption:

```
./run -d <plainTextFileName> <sessionKeyFileName> <publicKeyFileName> <privateKeyFileName>
```
code for decryption:
```
./run -d <plainTextFileName> <sessionKeyFileName> <publicKeyFileName> <privateKeyFileName>
```
Examples:
./run -d cipher.txt encrypted_session.key pubkey.pem key.pem decrypt.txt
./run -e plain.txt encrypted_session.key pubkey.pem key.pem decrypt.txt

Debug Options:
Debug comments can be turned on or off by changing the debug variable in the code. Below are the values and what each value turns on:

To check input set debug = 1;
To check decryption/encryption debug =2;






